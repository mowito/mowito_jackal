<launch>

    <arg name="scan_topic" default="front/scan" /> 
    <arg name="laser_frame" default="front_laser" />
    <arg name="odom_frame" default="odom" />
    <arg name="base_frame" default="base_link" />
    <arg name="map_frame" default="map" />

    <arg name="respawn" default="true"/>
    <arg name="debug" default="false"/>

    <arg name="world_name" default="mowito"/>
    <arg name="gui" default="true"/>


    <!--node pkg="tf" type="static_transform_publisher" name="ROSbot2_laser" args="0 0 0 3.14 0 0 base_link laser 10" /-->

    <!-- SIMULATION -->
    <!-- rosbot and the rviz-->
    <!--include file="$(env HOME)/mowito/launch/rosbot_rviz.launch">
        <arg name="rviz_file" value="rosbot.rviz"/>
    </include-->

    <!-- the world -->
    <!--include file="$(find rosbot_gazebo)/launch/rosbot_world.launch">
        <arg name="world" value="$(find rosbot_gazebo)/worlds/$(arg world_name).world"/>
        <arg name="gui" value="$(arg gui)"/>
    </include-->
   
    <!-- NAVIGATION -->
    <!-- mapping node-->
    <include file="$(find jackal_navigation)/launch/include/start_mw_mapping.launch">
        <arg name="scan_topic" value="front/scan" /> 
        <arg name="laser_frame" value="front_laser" />
        <arg name="odom_frame" value="odom" />
        <arg name="base_frame" value="base_link" />
        <arg name="map_frame" value="map" />
    </include>

    <!-- -for converting the odom msg to tf -->
    <!--node pkg="utils" type="msg_to_tf" name="msg_to_tf">
        <param name="odometry_topic" value="/odom"/>
    </node-->
    
    <!-- nav server-->
    <include file="$(find jackal_navigation)/launch/include/start_nav_server.launch">
        <arg name="mission_executive_config" value="$(find jackal_navigation)/config/mission_executive_config/standard_nav.yaml"/>
        <arg name="respawn" value="$(arg respawn)"/>
        <arg name="gp_debug" value="$(arg debug)"/>
        <arg name="ctrl_debug" value="$(arg debug)"/>
        <arg name="rcvr_debug" value="$(arg debug)"/>  
    </include>
     <node type="rviz" name="rviz" pkg="rviz" args="-d $(find jackal_navigation)/rviz/mowito_nav_no_map.rviz" />

</launch>